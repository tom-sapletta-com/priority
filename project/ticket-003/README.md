# ticket-003 — accountable autonomy cycle

Close the observe → route → invoke-or-abstain → evaluate loop inside this
package. Do not apply patches, do not invent planner or offer receipts, and do
not open dispatch while the policy lifecycle is `candidate`.
