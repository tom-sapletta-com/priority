# ticket-002 — publish 0.3.0 and bind generated receipts

Publish the indexed Evolutionary Priority DSL 0.3.0, keep fail-closed BLOCK
on the attached snapshot, and fix two package-integrity defects:

- Polish `ł` must not split router tokens
- `generated/verification-report.json` must cite current file SHA-256
