# Pinned pyqual contract

`default.yaml` is the exact `PyqualConfig.default_yaml()` text from
`semcod/pyqual` at `git:2fe7e4730a6c1d5b3326faaec4e41d455891e0ef`.

This pin proves we can parse the native configuration object. It does
not enable the quality-loop, does not replace `make verify`, and does
not promote this policy to `shadow`.
